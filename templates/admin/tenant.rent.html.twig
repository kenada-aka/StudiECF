{% extends "index.html.twig" %}

{% block body %}

<section id="hero" class="register d-flex align-items-end">
    <div class="carousel-item active">
        <div class="carousel-container">
            <h2 class="animate__animated animate__fadeInDown">{{ title }}</h2>
            <p class="animate__animated animate__fadeInUp">{{ subtitle }}</p>
        </div>
    </div>
</section>

<main id="main">
    <section class="main">
        <div class="container mt-4">
            {% if realties|length == 0 %}
                <div class="alert alert-warning" role="alert">Vous n'avez pas de location en cours !</div>
            {% else %}
                <table class="table table-striped">
                    <tbody>
                    {% for realty in realties %}
                        <tr>
                            <td>
                                <p>{{ ["","", "", "", "En attente de confirmation par le propriétaire", "Location confirmer !"][realty.statut] }}</p>
                                {% if realty.statut == 5 %}
                                    <a href="{{ path('owner.canceled', {idRent: realty.id}) }}" class="btn btn-primary">Déclarer un problème</a>
                                    <a href="{{ path('owner.accepted', {idRent: realty.id}) }}" class="btn btn-primary">Demander des travaux</a>
                                    <a href="{{ path('owner.accepted', {idRent: realty.id}) }}" class="btn btn-primary">Demander échelonner le loyer</a>
                                {% endif %}
                            </td>
                            <td>
                                {% for image in realty.images %}
                                    <img src="{{ asset('uploads/png/' ~ image.url) }}" /></td>
                                {% endfor %}
                            <td>
                                <p>{{ realty.title }}</p>
                                <p>{{ realty.description }}</p>
                            </td>
                            <td>
                                <p>{{ realty.rent }}</p>
                                {% if is_granted('ROLE_LOCATAIRE') %}
                                    <a href="{{ path('tenant.contact', {idOwner: realty.id}) }}" class="btn btn-primary">Contacter le propriétaire</a>
                                {% endif %}
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            {% endif %}
        </div>
    </section>
</main>

{% endblock %}