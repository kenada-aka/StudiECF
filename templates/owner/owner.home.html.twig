{% extends "index.html.twig" %}

{% block body %}

<section id="hero" class="register d-flex align-items-end">
    <div class="carousel-item active">
        <div class="carousel-container">
            <h2 class="animate__animated animate__fadeInDown">{{ title }}</h2>
            <p class="animate__animated animate__fadeInUp">{{ subtitle }}</p>
        </div>
    </div>
</section>

<main id="main">
    <section class="section">
        <div class="container mt-4">
            {% if realties|length == 0 %}
                <div class="alert alert-warning text-center" role="alert">Vous n'avez pas encore ajouté d'annonce</div>
            {% else %}
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>Statut</th>
                            <th>Photos</th>
                            <th>Titre</th>
                            <th>Description</th>
                            <th>Loyer</th>
                            <th>documents</th>
                            <th>Propriétaire</th>
                            <th>Locataire</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                    {% for realty in realties %}
                        <tr>
                            <td>
                                <p>{{ ["","En attente de validation par l'agence", "En attente de publication", "En attente de locataire", "Confirmer la location", "LOUER"][realty.statut] }}</p>
                                {% if realty.statut == 4 %}
                                    <a href="{{ path('owner.canceled', {idRent: realty.id}) }}" class="btn btn-primary">Annuler la demande</a>
                                    <a href="{{ path('owner.accepted', {idRent: realty.id}) }}" class="btn btn-primary">Confirmer la demande</a>
                                {% endif %}
                            </td>
                            <td>
                                {% if realty.images|length != 0 %}
                                    {% for image in realty.images %}
                                        <img src="{{ asset('uploads/png/' ~ image.url) }}"/>
                                    {% endfor %}
                                {% endif %}
                                <a href="{{ path('owner.upload.image', {idRent: realty.id}) }}" class="btn btn-primary">Ajouter une photo</a>
                            </td>
                            <td>{{ realty.title }}</td>
                            <td>{{ realty.description }}</td>
                            <td>{{ realty.rent }}€</td>
                            <td>
                                {% if realty.documents|length != 0 %}
                                    <ul>
                                    {% for document in realty.documents %}
                                        <li><a href="{{ asset('uploads/pdf/' ~ document.url) }}">{{ document.url }}</a></li>
                                    {% endfor %}
                                    </ul>
                                {% endif %}
                                <a href="{{ path('owner.upload.document', {idRent: realty.id}) }}" class="btn btn-primary">Ajouter un document</a>
                            </td>
                            <td>{{ realty.idOwner.lastname }} {{ realty.idOwner.firstname }}</td>
                            <td>
                                {% if realty.idTenant %}
                                    {{ realty.idTenant.lastname }} {{ realty.idTenant.firstname }}
                                    <a href="{{ path('owner.contact', {idOwner: realty.id}) }}" class="btn btn-primary">Contacter le locataire</a>
                                {% else %}
                                    <p>Il n'y a pas de locataire</p>
                                {% endif %}
                            </td>
                            <td>
                                <a href="{{ path('owner.update', {idOwner: realty.id}) }}" class="btn btn-primary">Modifier l'annonce</a>
                                <form method="POST" action="{{ path('owner.delete', {idOwner: realty.id}) }}">
                                    <input type="hidden" name="_method" value="DELETE"/>
                                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ realty.id)}}"/>
                                    <button class="btn btn-primary">Supprimer l'annonce</button>
                                </form>
                                {% if is_granted('ROLE_BAILLEUR_TIERS') %}
                                    {% if realty.statut == 2 %}
                                        <a href="{{ path('owner.post', {idOwner: realty.id}) }}" class="btn btn-primary">Publier l'annonce</a>
                                    {% endif %}
                                    <!--<a href="{{ path('admin.property.edit', {id: realty.id}) }}">Envoyer message au propriétaire</a>-->
                                {% endif %}
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            {% endif %}
        </div>
    </section>
</main>

{% endblock %}

